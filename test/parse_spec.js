var expect = require('chai').expect();

describe('URL PARSING', function () {

    describe('parseURLParam', function () {
        it('should return false if there are not parameters');
        it('should return dictionary of parameters if they do exist');
        it('should work with two or more parameters');
    });
    describe('getBaseURL', function () {
        it('should return base url');
    });
});